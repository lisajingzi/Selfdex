# Selfdex 全局指令模板（贴进 IDE User Rules）

> 复制本文件全部内容到 IDE 的「User Rules / 全局指令」设置（Cursor / Windsurf / 类似工具的 User Rules 都适用），所有工作区生效。

---

# 用户规则

Selfdex 根目录：`<在此填入你本机的 Selfdex 绝对路径，例如 ~/code/Selfdex>`

## 何时查 Selfdex

**触发**：用户第一人称表达观点 / 困惑 / 纠结 / 抱怨；涉及个人立场、情绪、关系、人生方向、价值观；提到具体人名 / 过往事件；说「翻 Selfdex / 翻 selfdex / 查 Selfdex」。

**动作**：`read_file: <Selfdex>/INDEX.md`，按 INDEX 指引行动。

**🔒 红线**：禁止跳过 INDEX.md 凭印象判断主题归属或编造用户立场。

## 何时写入 Selfdex

**触发词**（大小写不敏感）：

- 「记到 Selfdex」/「记到 selfdex」
- 「存到 Selfdex」/「存到 selfdex」

**动作**：`read_file: <Selfdex>/docs/flow.md`，按 §3 写入流程执行（含确认协议）。

**🔒 红线**：禁止跳过确认协议直接落盘。

## 操作前必读

- `<Selfdex>/docs/flow.md`（流程总指挥）
- `<Selfdex>/docs/redlines.md`（完整红线清单）

---

*存档：`<Selfdex>/docs/user-rules-template.md`*
